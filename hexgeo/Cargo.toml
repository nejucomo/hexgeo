[package]
name = "hexgeo"
version = "0.1.0"
edition.workspace = true

[dependencies]
derive_more = { workspace = true, features = ["from", "into"] }
emath.workspace = true

[lib]
test = false
doctest = false
