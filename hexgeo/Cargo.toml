[package]
name = "hexgeo"
version = "0.1.0"
edition.workspace = true

[dependencies]
derive-new.workspace = true
derive_more = { workspace = true, features = ["from", "into"] }

[lib]
test = false
doctest = false
